const num1 = 50;
const num2 = 30;
let isGreater;

if (num1 > num2) {
  isGreater = true;
} else {
  isGreater = false;
}

// OR TERNARY

isGreater = num1 > num2 ? "IS GREATER!" : "IS NOT GREATER";
//          condition       TRUE            FALSE

isNum1Fifty = num1 === 50 ? "YES" : "NO";

console.log(num1 === 50 ? "YES" : "NO");

const day = true;

console.log(day ? "It is day-time" : "It is night-time");
