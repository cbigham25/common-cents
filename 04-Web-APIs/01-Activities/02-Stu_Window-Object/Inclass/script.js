// Logs window object
console.log(window);

// logs document in the window (all the html)
console.log(window.document);

// logs root element
console.log(document.documentElement);

// logs head of the current document
console.log(document.head);

